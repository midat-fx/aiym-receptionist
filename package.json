{
  "name": "aiym-receptionist",
  "version": "0.1.0",
  "description": "Aiym — AI receptionist for small businesses: books appointments from Telegram text and voice on a free Cloudflare Worker",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "test": "vitest run",
    "test:dates": "tsx scripts/test-dates.ts",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@cloudflare/vitest-pool-workers": "^0.18.6",
    "@cloudflare/workers-types": "^5.20260714.1",
    "tsx": "^4.23.1",
    "typescript": "^5.5.4",
    "vitest": "^4.1.0",
    "wrangler": "^4.0.0"
  }
}
