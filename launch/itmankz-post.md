# Пост в @itmankz (show-and-tell)

Правила канала: без цен и без лендинга, только показать проект и попросить фидбек.
Публиковать в будний день 11:00–15:00. Перед постом прочитать закреп канала.
`{VIDEO_URL}` — ссылка на демо-видео (этап 8). `@aiym_admin_bot` — заменить, если бот другой.

---

Сделал пет-проект: **Айым — AI-администратор для салонов красоты.** Клиент пишет или отправляет голосовое «запишите завтра к трём» — она отвечает голосом, находит свободный слот и бронирует.

Под капотом: Cloudflare Workers + D1 + KV, Whisper (Workers AI) для STT, Gemini function calling для интентов, ElevenLabs TTS. Главный принцип: LLM только понимает запрос, а расписанием управляет детерминированный движок с тестами — двойная бронь невозможна по построению. Инфраструктура — $0/мес на фритирах.

Живое демо: https://aiym.faizov-midat.workers.dev/demo (голосовое тоже можно).
Код: https://github.com/midat-fx/aiym-receptionist

Где сломается в первую очередь? Фидбек очень welcome.

---

**Порядок запуска (важно):** этот пост — ДО рассылки салонам. Сообщество = бесплатный нагрузочный тест: если что-то падает под наплывом, лучше узнать здесь, а не на первом клиенте.
